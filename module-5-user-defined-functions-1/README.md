# 🧩 Module 5 — User Defined Functions I

> **Start here:** copy your finished Module 4 `game.py` into this folder first.

## What you're building

Extract **`show_scene()`** and **`get_choice()`** from `main()`. Same game as Module 4 — just better organized.

## Why it matters

Functions cut duplication. You'll reuse these every turn.

## Acceptance criteria

- [ ] `show_scene()` handles display
- [ ] `get_choice()` handles input
- [ ] Behaves like Module 4
- [ ] `python3 -m pytest module-5-user-defined-functions-1/`

## Explain your code

2–3 sentences here:

> What does each function return? Why validate input inside `get_choice()`?

Then message Kaosi: **"Module 5 done, ready for a PR."** → [SUBMITTING_A_PR.md](../SUBMITTING_A_PR.md)

---

*Your answer goes above ↑ and in the PR description.*
